#include "Array_Iterator.h"

/*Array_Iterator::Array_Iterator(Array* p)
{
	this->p = p;
	pos = 0;
}

void Array_Iterator::next()
{
	if(p->size()-1 > pos)
		pos++;
}

void Array_Iterator::prev()
{
	if(pos > 0)
		pos--;
}

void* Array_Iterator::get()
{
	return p->getData(pos);
}

bool Array_Iterator::isEqual(I_Iterator* tmp)
{
	return (((Array_Iterator*)tmp)->p == p && ((Array_Iterator*)tmp)->pos == pos);
}*/
